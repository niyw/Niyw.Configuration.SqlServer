# v1.0
## 功能
- 提供配置基本功能，在数据库中添加配置项，应用程序能获取配置项
- 数据库配置项需遵循Key-Value规则，其中Key必选遵循":规则"，即键值如需分级，需用冒号“:”分割

# v1.1
## 更新
- 支持自动从数据库获取最新配置。这是默认行为；
- 通过SqlServerConfigurationSource构造函数，可执行配置获取行为；默认每5秒钟从数据库获取最新配置；对应的参数为：
- reloadOnChange：默认为true,当设置为False时，不再自动获取最新配置；
- refreshInterval：默认为5，单位秒；
